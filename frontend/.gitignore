# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Real card photos used to validate scan-hash matching (sourced ad hoc from
# the web for testing — not ours to redistribute, and not needed by anyone
# who isn't actively running the validation harness).
public/dev-fixtures/

# Scan descriptor build scratch: downloaded card art and generated
# bundles. Regenerate with the scan-descriptors workflow; never source.
.scan-build/
